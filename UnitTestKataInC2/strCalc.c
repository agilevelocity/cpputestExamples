#include <stdio.h>
#include <string.h>

// Input a string containing integer numbers and returns the sum.
// Return -1 if the string is null
// Return 0 if the string is empty
int Add(char *strNumbers)
{
	if (strNumbers == 0) {
		return -1;
	}
	if (strlen(strNumbers) == 0) {
		return 0;
	}
    return (-1);
}


